import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  imageBackground: {
    width: '100%',
    height: '100%',
    alignItems: 'center',
    justifyContent: 'space-around'
  },
  middleContainer: {
    width: '100%',
    alignItems: 'center'
  },
  textInputContainer: {
    width: '80%',
    marginTop: 30
  },
  footerText: {
    position: 'absolute',
    bottom: 15
  }
});
